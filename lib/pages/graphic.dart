import 'package:flutter/material.dart';

class GraphicPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Graphic Page'),
      ),
      body: Center(
        child: Text('Welcome to Graphic Page'),
      ),
    );
  }
}
